# pipe 프로젝트

## 배포
- 배포 방법: GitHub main 브랜치 push → GitHub Actions 자동 빌드 → GitHub Pages 배포
- 배포 URL: https://sjidok750-creator.github.io/pipe/
- 워크플로우: `.github/workflows/deploy.yml`
- 빌드 명령: `npm run build` (출력: `dist/`)
- base path: `/pipe/` (vite.config.ts)
- "배포해줘" 요청 시 `git push origin main` 실행 (Actions가 자동으로 빌드+배포, 2~4분 소요)

## 한글(.hwpx) 문서 작성 표준

앱에서 생성하는 **모든** 한글문서에 적용한다.
서식 견본: `D:\2026년 과업\A. 인천광역시 송수관로 정밀안전진단\제3장 안전성 및 내진성능평가 결과(인천 송수관로).hwpx`
상세 지침: 구글드라이브 「한글문서(HWPX) 편집 작업지침 v2.md」

| # | 지침 | 구현 |
|---|---|---|
| 1 | **0.0절**이 바뀔 때는 쪽을 바꿔도 됨 | `b.heading()` — 자동 쪽 나눔 (문서 첫 절은 제외) |
| 2 | **0.0.0절**은 쪽을 바꾸지 말고 이어 씀 | `b.subheading()` — 쪽 나눔 없음 |
| 3 | 표·그림 제목이 개체와 갈라지지 않게 | `b.tableWithCaption()` / `b.figureWithCaption()` |
| 4 | 표가 넘치면 머리행과 함께 다음 쪽으로 분리 | `pageBreak="CELL"` + `repeatHeader="1"` (기본) |
| 5 | 표 선두께 — **안 0.12mm / 바깥(테두리) 0.4mm** | `BF_GRID` 12종 (borderFill id 8~19) |
| 6 | **표제목은 항상 표 위** | `tableWithCaption()` |
| 7 | **그림제목은 항상 그림 밑** | `figureWithCaption()` |

### 반드시 지킬 것 (지침서 v2 요약)
- 셀 `lineWrap="BREAK"` — `SQUEEZE`는 글자가 겹친다
- 글자를 바꾼 문단은 `linesegarray`를 **제거**(한글이 재계산) — 남기면 줄이 겹쳐 그려진다
- `zOrder`는 0..N-1 고유, `hp:tbl id`/`instid`도 고유
- 쪽 나눔 직전 빈 문단·연속 빈 문단 제거 → 빈 쪽 생김 (`_dropTrailingEmpty()`)
- borderFill·스타일은 **새로 만들지 말고 템플릿 것을 재사용**
  (신설 시 id는 연속 번호 + `itemCnt` 갱신 — 한글은 IDRef를 배열 인덱스로 해석)
- `mimetype`은 zip 첫 항목·무압축(STORED)

단위: 1mm ≈ 283.465 HWPUNIT, 1pt = 100 HWPUNIT, A4 = 59528 × 84188

## ⚠ 근거 계층 (관로 구조검토)

이 앱의 용도는 **정밀안전진단**이며, 구조검토의 직접 근거는 세부지침이다.

```
1차 : 「시설물의 안전 및 유지관리 실시 세부지침(안전점검·진단 편) 해설서」
        제11장 상수도 11.5.2  (11-132 ~ 11-138)
        → 토압식 / 링휨식 / 허용기준표 / 관두께 적용규칙 / 평가등급
2차 : 상수도시설기준 2004 [참고-4.2.1]   (지침이 지정한 계산식 출처)
        "관로의 안전성검토 식은「상수도시설기준, 환경부」에 제시된 식의
         사용을 원칙으로 한다" (11-133)
        → E′ 토질별 표, 관종별 허용응력, PVC·PE 계산식 등 지침 미수록분
참고 : AWWA M11, 구조물기초설계기준 2016
        지침이 참고자료로 지정한 범위 내에서만 (11-133)
금지 : 현행 KDS 57 계열을 관로 구조계산 근거로 인용
        → 해당 규정이 존재하지 않음 (해설편 1,364쪽 전수검색:
          프리즘/Prism/AWWA M11/DIPRA/Iowa/편평률/링휨 모두 0회)
```

### 인용 가능한 원문 (저장소 내)

1. `11. …상수도편(해설서).hwpx` — **세부지침 해설서 제11장** (구조검토 1차 근거)
2. `상수도설계기준.PDF` — **KDS 57 00 00 : 2022** 관보 원문 (199쪽, 텍스트 추출 가능)
3. `상수도+시설기준+2004.pdf` — 구 상수도시설기준 2004 (1,095쪽, **스캔본**)
4. `상수도설계기준_해설편(2025)_개정2판.pdf` — 2025 해설편 (1,363쪽, 텍스트 추출 가능)
5. `기존 시설물(상수도) 내진성능 평가요령.pdf` — 내진 검토 근거

**그 외 저장소의 모든 PDF는 파생 참고문서이며 공신력이 없다. 근거로 인용 금지.**

### 세부지침 .hwpx 판독 방법
본문은 `Contents/section1.xml`, 수식은 `<hp:script>` 개체에 들어 있다.
텍스트만 뽑으면 수식이 **빈 줄로 보이므로** 반드시 수식 개체를 함께 추출할 것:

```python
import zipfile, re
z = zipfile.ZipFile('11. ….hwpx')
xml = z.read('Contents/section1.xml').decode('utf-8')
eqs = re.findall(r'<hp:script[^>]*>(.*?)</hp:script>', xml, re.S)
# 한글 주석이 깨져 보이면: s.encode('latin-1','ignore').decode('cp949','ignore')
# ※ 숫자·기호는 깨지지 않으므로 상수 판독에는 지장 없음
```

### 2004 스캔본 판독 방법
`extract_text()`가 0바이트를 반환하지만 **읽을 수 없는 것이 아니다.**
페이지를 이미지로 추출해 직접 판독할 것:

```python
from pypdf import PdfReader
from PIL import Image
import io
r = PdfReader('상수도+시설기준+2004.pdf')
im = Image.open(io.BytesIO(list(r.pages[203].images)[0].data)).convert('L')
im = im.resize((im.width*2, im.height*2), Image.LANCZOS)   # 2배 확대 시 선명
im.save('out.png')
```
※ **PDF 페이지 = 인쇄 페이지 + 29** (예: 인쇄 p.175 = PDF p.204)
※ 관로 구조검토 [참고-4.2.1]은 인쇄 p.171~175 (PDF p.200~204)

## ⚠ 인용 금지 문서

`강관_주철관_설계기준_변천_분석.pdf` — **근거로 인용하지 말 것.**
저장소 내 요약 PDF이며 「상수도시설기준(2004)」 원문과 대조한 결과 다음 오류가 확인됨:

| # | PDF 기재 | 원문 |
|---|---|---|
| 1 | Kb(지지각 90°) = 0.235 | **0.157** (참고표-4.2.4) |
| 2 | 구 기준 허용변형률 "라이닝 무관 5%" | 2004 원문은 **도장 5% / 모르타르 3%** (참고표-4.2.6) |
| 3 | 강관 탄성계수 206,000 MPa | **200,000 N/mm²** |
| 4 | 구 기준 주철관 허용응력 98 MPa | 원문에 **해당 값 없음** (조합응력 방식만 존재) |
| 5 | 강관 허용응력 137.2 MPa (표 5.9.1) | **140 MPa** (참고표-4.2.5, 조항번호도 다름) |

> **② 허용 편평률 — 기준별로 다르다 (둘 다 맞다)**
> · 2004 시설기준 기준으로는 **도장 5% / 모르타르 3%** 분기가 맞다 (참고표-4.2.6).
> · **세부지침 기준으로는 라이닝 무관 5% 단일이 정본**이다 (11-134 [해설 표 11.5.1]:
>   "관체 변형량 — 관경의 5% 미만").
> · **진단업무는 세부지침이 우선**하므로 이 앱은 5% 단일값을 적용한다.

같은 이유로 `링휨응력_산정방식_비교분석.pdf`, `상수도_설계기준_변천_종합분석(2004_vs_2025).pdf`,
`엑셀_앱_비교분석_보고서.pdf` 도 **파생 문서**이므로 1차 근거로 사용 금지.

### 서술 내용이 원문 대조로 반증된 문서
`analysis_text.txt`, `상수도_설계기준_변천_종합분석(2004_vs_2025).pdf` — 다음이 사실과 다름이 확인됨:

| # | 기재 내용 | 반증 |
|---|---|---|
| 1 | "구 기준 = Marston, 현행 = Prism" | 현행 KDS·해설편에 매설관 구조계산 규정 자체가 **없음** |
| 2 | "Prism은 Marston 대비 토압이 크게 산정된다" | 조건부이며 일반화 불가 (B·H 관계에 따라 역전) |
| 3 | 강관 허용응력 137.2 MPa | 원문 **140 MPa** |
| 4 | 허용처짐 "라이닝 무관 5%"를 현행 신설로 서술 | 세부지침에 원래 있던 값 |
| 5 | 현행 차량하중을 "DB-24"로 서술 | 현행 도로교설계기준은 **KL-510** (단, 세부지침 11-134는 DB-24 기준) |

기준 확인이 필요하면 위 「인용 가능한 원문」을 직접 확인할 것.

## 계산 경로 — 세부지침 단일 (SET_A/SET_B 이원 구조 폐지)

`codeStandard`·`reviewMode`·`primaryCode`·`calcSteelPipeDual()`·`resolveAllowable()`은
**모두 제거되었다.** 되살리지 말 것. 계산 경로는 하나뿐이다.

### 강관 (11-134 ~ 11-136)
| 항목 | 값·식 | 근거 |
|---|---|---|
| 상부 토압 | H≤2m: `γt·H` / H>2m: `Cd·γt·B` | 11-134 |
| 굴착부 폭 | `B = 2D + 100` **[cm]** | 11-134 |
| 토압계수 | `kμ′` = φ=30°에서 유도 (0.19245) | 11-134 |
| 노면하중 | Kögler 분산 `2nP(1+i)/{…}` | 11-134 |
| 충격계수 | H<1.5→0.5 / 1.5~6.5→0.65−0.10H / **>6.5→0** | 11-134 |
| 내압 | `σt = P·D/(2t)` | 11-134 |
| 외압 휨 | `σb = 2/(f·z)·W·[KbR²EI+(a·Kb−b·Kx)E′R⁵]/[EI+0.061E′R³]` | 11-135 |
| 링휨 계수 | `a=0.06146, b=0.08303` — 원문 표기 0.061/0.083은 **반올림 표시** | 11-135 표 4열 |
| 변형률 | `ε = 2Kx·W·R⁴/(EI+0.061E′R³)·(1/D)·100` | 11-136 |
| 좌굴 | `qa = (1/FS)·√(32RwB′E′EI/D³)`, `B′=0.15+0.041(H/D)` | 11-136 |
| 설계계수 | `FS = 2.5 (H/D≥2) / 3.0 (H/D<2)` | 11-136 |
| E / E′ | `2.1×10⁶` / `28` kg/cm² | 11-135 |
| 허용응력 | **상시 140 / 일시 210 MPa** (STWW 400, 강종별 분기 없음) | 11-134 표 11.5.1 |
| 허용 변형 | **관경의 5% 미만** (라이닝 무관) | 11-134 표 11.5.1 |

### 주철관 (11-137)
`2.5·σts + 2.0·σtd + 1.4·σb < S (=420 MPa)` — **내압·외압을 분리하지 않고 조합**
`σb = 6(Kf·Wf + Kt·Wt)R²/t²`, Kf 40~180°(90°=**0.160**), Kt=0.011 (관저 기준)

### 반드시 지킬 것
- **단위**: 전 과정 `cm / kg / kg/cm²` 원단위. `B = 2D+100`은 **cm**다.
  mm로 해석하면 Wv가 약 26% 낮게(**위험측**) 나온다. 검증: DN500 → B=200cm
- **D는 관 내경**이다 (EQ4 "D : 관 내경(mm)", EQ10 동일). 외경 대입 시 과대평가.
  `R = D/2 + t` 는 D=내경이므로 **외반경과 일치**한다.
  `Do/2 + t` 로 쓰면 R이 관 벽 바깥에 놓여 성립하지 않는다.
- **링휨 계수**: 원문 수식의 `0.061Kb − 0.083Kx` 는 반올림 표기다.
  11-135 표 4열의 인쇄된 계산값과 대조하면 `0.06146 / 0.08303` 이 정본
  (절대오차 0.000070 vs 0.000297). 분모의 `0.061` 은 원문 표기 그대로 사용.
- **안전율**: 변형률도 `허용/발생` 비로 환산해 SF_min 에 포함할 것.
  누락하면 종합 N.G. 인데 등급 a 가 같은 보고서에 인쇄된다.
- **지지각 이관**: 지침 표에 없는 구 키는 **가장 보수적인 값**으로 보정하고
  `beddingCoerced` 로 사용자에게 알릴 것 (강관 deg60 / 주철관 deg40).
  조용히 기본값으로 떨어뜨리면 판정이 뒤집힐 수 있다.
- **하중 조합**: 외압 검토 시 내부 수압 없음 / 내압 검토 시 외부 하중 없음 (11-134 ②)
- **관두께**: `min(실측 최소, 기준)` — 실측값 미입력 시 기준 두께 + 화면 명시 (11-134 ②)
- 허용응력을 하드코딩하지 말 것. `STEEL_ALLOW`·`DI_COMBINED` 참조 + `allowSource` 동반 반환
- 회귀 검증: `node verify_guideline.mjs`

※ KDS 57 10 00에는 링휨 허용응력 명시 조항이 **없다**(§3 = "재료 — 내용 없음").
  코드·보고서에서 §3.2/§3.4 등을 인용하지 말 것.
